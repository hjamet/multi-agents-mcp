# 📊 Shared Results Explorer

You have access to a **Shared Results Explorer** to document your work, visualize results, and share data.

## How to use

1.  **Create a Page**: Create a Python file in `mamcp-streamlit/subpages/`.
    *   Example: `mamcp-streamlit/subpages/01_analysis_v1.py`
    *   Example: `mamcp-streamlit/subpages/experiment_results.py`
2.  **Navigation**: The system **automatically** detects your file and adds it to the Dashboard sidebar.
3.  **Content**: Write standard Streamlit code inside a `main()` function.

## Example Page

File: `mamcp-streamlit/subpages/01_latency_test.py`

```python
import streamlit as st
import pandas as pd

def main():
    st.header("Latency Test Results")
    st.write("Analysis of the new caching mechanism.")
    
    data = {"Request": [1, 2, 3], "Time (ms)": [120, 60, 55]}
    st.bar_chart(pd.DataFrame(data).set_index("Request"))

# No need for if __name__ == "__main__", but it's good practice.
```

## ⚠️ Constraints

*   **Do NOT edit `dashboard.py`**: It manages the navigation logic. Just add files to `subpages/`.
*   **Do NOT use `st.set_page_config`**.
*   **One File = One Topic**: Keep your results organized in separate files.
