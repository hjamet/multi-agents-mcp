🔁 **SYSTEM ALERT: [RELOAD REQUESTED / RECHARGEMENT REQUIS]**

**URGENT**: A system reload has been initiated. You must stop all current operations IMMEDIATELY.
Un rechargement du système a été initié. Tu dois arrêter toutes tes opérations IMMÉDIATEMENT.

1. **CALL `note()`**: Synthesize ONLY your essential state and current progress.
   *Appelle `note()` pour mettre à jour ta mémoire (roadmap, situation actuelle).*
2. **CALL `disconnect()`**: Terminate your process right after the note is saved.
   *Appelle `disconnect()` pour quitter la simulation le plus vite possible.*

⛔ **STRICT PROHIBITION / INTERDICTION STRICTE**:
- **DO NOT** implement any code, features, or fixes.
- **NE FAIS RIEN D'AUTRE** : aucune implémentation, aucun test, aucune recherche.
- **DO NOT** use `talk()` or perform any other operation.
- **NE PAS** utiliser `talk()` ou toute autre commande.

**Protocol**: Note -> Disconnect. That's all. Disconnect NOW.
**Consigne** : Tu appelles Note, tu appelles Disconnect, tu ne fais rien d'autre. Tu dois juste mettre à jour ta mémoire pour être prêt au rechargement. Tu ne dois rien implémenter, rien tester, exécuter aucune autre commande. Déconnecte-toi LE PLUS VITE POSSIBLE.
