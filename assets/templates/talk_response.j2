--- 🌍 GLOBAL CONTEXT ---
{{ context }}
-------------------------

--- 🎭 ROLE REMINDER ---
You are **{{ name }}**.
{{ role_snippet }}
------------------------

--- 📨 MESSAGE HISTORY (SINCE YOUR LAST TURN) ---
{% for m in messages %}
{% if m.public %}
- **{{ m.from }}** a dit à tout le monde : "{{ m.content }}"
{% else %}
    {% if m.target == name %}
- **{{ m.from }}** t'a dit (confidentiel) : "{{ m.content }}"
    {% elif name in m.audience %}
- **{{ m.from }}** a chuchoté à {{ m.target }} (tu as entendu) : "{{ m.content }}"
    {% elif m.from == name %}
- **Tu** as dit à {{ m.target }} : "{{ m.content }}"
    {% else %}
- **{{ m.from }}** a dit à {{ m.target }} : "{{ m.content }}"
    {% endif %}
{% endif %}
{% else %}
(Aucun nouveau message.)
{% endfor %}
-------------------------------------------------

--- ⚠️ YOUR TURN: ACTION REQUIRED ---
It is your turn to speak. You MUST NOT STOP.
1. **Analyze** the messages and your role.
2. **Decide** who to speak to next based on your Connections table below.
3. **Call `talk(...)`** to send your message and pass the turn.

**Arguments for `talk`:**
- `message`: Your content.
- `public`: True (Everyone hears) or False (Only Target + Audience check capabilities).
- `next_agent`: The exact name of the agent who speaks next (See Connections).
- `my_name`: **"{{ name }}"** (You MUST pass your own name to verify identity).
- `audience`: (Optional) List of other agents to include in private messages.

--- 🔗 YOUR CONNECTIONS & STRATEGY ---
### 👥 POPULATION & CONNECTIONS
Use this table to decide who to speak to.

**Communication Guide:**
- `public=True`: Everyone sees the message. (If you have Public capability)
- `public=False, next_agent="Name"`: Private message to Next Agent. (Requires Direct Connection or OPEN Mode)
- `audience=["A", "B"]`: Also visible to A and B. (Requires Direct Connection/OPEN Mode for each)

| Agent Name | Public Role | Reachability | Notes / Context |
| :--- | :--- | :--- | :--- |
{% for a in agent_directory %}
| **{{ a.name }}** | {{ a.public_desc }} | {{ a.status }} | {{ a.note }} |
{% endfor %}
------------------------------------------
